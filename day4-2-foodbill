#You are going to write a program that will select a random name from a list of names. The person selected will have to pay for everybody's food bill.

import random

# 🚨 Don't change the code below 👇
test_seed = int(input("Create a seed number: "))
random.seed(test_seed)

# Split string method
names_string = input("Give me everybody's names, separated by a comma. ")
names = names_string.split(", ")
# 🚨 Don't change the code above 👆

#Write your code below this line 👇
items = len(names) # get the total number of items in list.
random_pick = random.randint(0, items -1) #Generate random numbers between 0 and the last index.
will_pay = names[random_pick]
print(f"{will_pay} is going to buy the meal today!")

# Angela's solution with function .choice
person_who_will_pay = random.choice(names)
print(person_who_will_pay + " is going to buy the meal today")
